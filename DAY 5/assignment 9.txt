1.
ans.
select onum , cname from orders ,customers  where customers.cnum = orders.cnum;

2.
ans. 
select sname,  cname , onum from customers c , orders o , salespeople s where o.snum = c.snum and o.snum = s.snum;

3.
ans.
select cname , sname , comm from customers c, salespeople s where c.snum = s.snum and comm > 0.12;

4.
ans. 
Select sname, amt * comm from orders, customers, salespeople
where rating > 100 and   salespeople.snum = customers.snum and salespeople.snum = orders.snum and customers.cnum = orders.cnum;